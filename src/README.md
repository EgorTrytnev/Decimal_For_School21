# s21_decimal Library - Level 4 Implementation

## Структура проекта

```
src/
├── s21_decimal.h              # Заголовочный файл библиотеки
├── s21_helpers.c              # Вспомогательные функции
├── s21_is_less.c              # Функции сравнения
├── s21_is_greater.c
├── s21_is_less_or_equal.c
├── s21_is_greater_or_equal.c
├── s21_from_int_to_decimal.c  # Функции конвертации
├── s21_from_decimal_to_int.c
├── tests/                     # Тесты
│   ├── test_comparison.c      # Unit-тесты с Check
│   └── test_simple.c          # Простые тесты
├── docs/                      # Документация
│   ├── LEVEL4_README.md       # Документация функций
│   └── MAKEFILE_USAGE.md      # Руководство по Makefile
├── build/                     # Сборка
│   └── Makefile               # Makefile для сборки
└── tmp/                       # Временные файлы (автоматически создается)
    ├── *.o                    # Объектные файлы
    ├── *.gcno                 # Gcov файлы
    ├── *.gcda
    ├── *.gcov
    ├── coverage/              # Отчеты покрытия
    └── test_*                 # Исполняемые файлы тестов
```

## Быстрый старт

### Из корня проекта:
```bash
# Сборка и тестирование
make all

# Только тестирование
make test_clean

# Тестирование с Check
make check_clean

# Генерация отчета покрытия
make gcov_report
```

### Из директории src/build:
```bash
cd src/build

# Сборка библиотеки
make s21_decimal.a

# Запуск тестов
make test_clean
make check_clean

# Генерация отчета покрытия
make gcov_report
```

## Реализованные функции

### Функции сравнения
- `s21_is_less` - сравнение "меньше" (<)
- `s21_is_greater` - сравнение "больше" (>)
- `s21_is_less_or_equal` - сравнение "меньше или равно" (<=)
- `s21_is_greater_or_equal` - сравнение "больше или равно" (>=)

### Функции конвертации
- `s21_from_int_to_decimal` - преобразование из int в decimal
- `s21_from_decimal_to_int` - преобразование из decimal в int

## Особенности

- **Автоматическая очистка**: Все временные файлы создаются в `src/tmp/` и автоматически очищаются
- **Изолированная сборка**: Объектные файлы и библиотеки не загрязняют исходный код
- **Полное покрытие тестами**: 18 unit-тестов с Check библиотекой
- **Соответствие стандартам**: C11, POSIX.1-2017, Google Style
- **Готовность к интеграции**: Все функции готовы для использования командой

## Документация

- `docs/LEVEL4_README.md` - подробная документация функций
- `docs/MAKEFILE_USAGE.md` - руководство по использованию Makefile

## Требования

- GCC компилятор
- Check библиотека (для тестов)
- lcov и genhtml (для отчетов покрытия)

## Статус

✅ **Завершено**: Все функции реализованы и протестированы  
✅ **Протестировано**: 18/18 тестов проходят успешно  
✅ **Документировано**: Полная документация и комментарии в коде  
✅ **Готово к интеграции**: Совместимо с остальными частями проекта
